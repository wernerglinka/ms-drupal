{% from "../partials/text.njk" import text %}
{% from "../partials/ctas.njk" import ctas %}

<section class="section-intro {% if params.hasTopPadding %}hasTopPadding{% endif %} {% if params.hasBottomPadding %}hasBottomPadding{% endif %}">
  <div class="content">
    {% set info = params %}
    {{ text(info.text) }}

    {% if info.hasCtas %}
      {{ ctas(info.ctas) }}
    {% endif %}
  </div>
</section>