{% from "../partials/ctas.njk" import ctas %}
{% from "../partials/text.njk" import text %}

{# this section receives props params and site #}

<section 
  class="section-cta-banner {% if params.commons.hasTopPadding %}hasTopPadding{% endif %} {% if params.commons.hasBottomPadding %}hasBottomPadding{% endif %} {% if params.commons.colorIsDark %}isDark{% endif %}"
  style="{% if params.commons.backgroundColor %}background-color: {{ params.commons.backgroundColor }};{% endif %}"
>
  <div class="content">
    {% set info = params %}
    {{ text(info.text) }}

    {% if info.hasCtas %}
      {{ ctas(info.ctas) }}
    {% endif %}
  </div>
</section>